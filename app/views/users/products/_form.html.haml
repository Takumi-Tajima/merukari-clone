= simple_form_for([:users, product]) do |f|
  -# TODO: カテゴリーの一覧取得方法インスタンス作成しているのはOKなのか…
  = f.input :category_id, collection: Category.default_order, prompt: 'カテゴリーを選択してください'
  = f.input :name
  = f.input :description
  = f.input :price
  = f.button :submit, class: 'btn-primary'
