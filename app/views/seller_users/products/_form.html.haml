= simple_form_for([:seller_users, product]) do |f|
  = f.input :category_id, collection: Category.for_select, prompt: 'カテゴリーを選択してください'
  = f.input :name
  = f.input :description
  = f.input :price
  = f.button :submit, class: 'btn-primary'
